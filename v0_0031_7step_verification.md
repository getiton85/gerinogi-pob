# v0.0031 직업 스킬·장비·대시보드 연결 검수

## 변경 범위

- ON 직업 전체 스킬 행의 평균 기대피해를 대시보드에 표시
- 저장 기준 장비 대비 직업 스킬 장비지수 표시
- 장비 비교에 직업 스킬 평균과 변화율 표시
- 공통 스킬 피해 이중 적용 제거
- 브레이크·차지·자원 소모·연계 전용 배율 조건 분리

## 7단계 검수

① 기능 구현 검수: **PASS**

- 대검전사 26개 표시 행이 모두 직업 스킬 요약에 포함됨
- 기준 장비 평균 기대피해 `275,808.05398530455`
- 기준 장비 스킬 지수 `100.00`

② 체크리스트 검수: **PASS**

- `core.js`, `app.js`, `index.html`, `version.json`, `PROJECT.md` 반영
- 버전 및 캐시 쿼리 `v0.0031` 일치
- placeholder, TODO, 임시 데이터 추가 없음

③ 숫자/계산 결과 대조: **PASS**

- 대검전사 표시 행 수 `26`
- 모든 행의 기대피해가 유한한 양수
- 비교 무기 적용 평균 기대피해 `290,172.5994661856`
- 현재 대비 변화율 `+5.208167518431651%`

④ 역검증: **PASS**

- 저장 기준 장비를 다시 계산했을 때 직업 스킬 장비지수 `100.00`
- 공통 `skillDamagePct`는 공통 배율에서 한 번만 적용
- 전용 피해는 해당 스킬 속성 또는 태그가 없으면 적용하지 않음

⑤ UI 검수: **미검증**

- 로컬 정적 서버는 실행했으나 인앱 브라우저의 로컬 주소 접근 정책으로 실제 클릭·화면 렌더링 검수 불가
- HTML/JavaScript DOM 연결과 문법 검사는 통과

⑥ 연결 검수: **PASS**

- 대시보드가 `classSkillSummary()`를 호출해 ON 직업을 자동 선택
- 장비 비교가 동일 함수로 현재 장비와 비교 장비를 각각 재계산
- 대검전사 ON 상태에서 무기 변경이 스킬 평균과 변화율에 반영됨

⑦ 회귀 테스트: **PASS**

- `node_test.js`
- `norm_test.js`
- `v23_case_test.js`
- `v26_expected_damage_test.js`
- `node --check core.js`
- `node --check app.js`
- `git diff --check`

## 종합 판정

UI 실화면 검수가 미검증이므로 전체 작업을 완료로 판정하지 않는다.
